version: '3.8'

services:
  app:
    build: .
    container_name: jwt-auth-lab
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app/app
      - db_data:/app/data
    environment:
      - FLASK_ENV=development
    restart: unless-stopped

volumes:
  db_data:
